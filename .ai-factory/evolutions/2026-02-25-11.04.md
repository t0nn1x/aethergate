# Evolution: 2026-02-25 11:04

## Intelligence Summary
- Patches analyzed: 23 (`.ai-factory/patches/*.md`)
- Recurring patterns (top): `#godot` (23), `#ui` (13), `#regression` (8), `#input` (7), `#cursor` (5), `#mouse` (4)
- Tech stack: Godot 4.6 + GDScript (`.ai-factory/DESCRIPTION.md`)
- New patch analyzed: `2026-02-25-10.42.md`

## Improvements Applied

### ai-factory.fix
- Added investigation traps for requested-vs-resolved move-target authority mismatch in click-to-move pipelines.
- Added hold-retarget cache guard to store accepted/resolved coordinates rather than pre-resolution input.
- Added pointer-anchor compensation trap to avoid hardcoded desktop offsets masking coordinate bugs.
- Added verification rule to explicitly validate pointer marker and final player arrival alignment to resolved target.
- Driven by patch: `2026-02-25-10.42.md`.

### ai-factory.review
- Added Godot checklist items to verify click-to-move uses a single authoritative target across request resolution, signal payload, pointer rendering, and arrival.
- Added checklist item to verify hold-retarget caches accepted/resolved targets.
- Added checklist item to ensure pointer anchor offsets are zero/intentional and not silent drift workarounds.
- Driven by patch: `2026-02-25-10.42.md`.

### ai-factory.commit
- Added commit-time guard to stage move request service + input component + pointer marker updates together for alignment fixes.
- Added guard to verify staged signal payloads use resolved/accepted targets (not raw clicks).
- Added guard to include related `.ai-factory/patches`/evolution artifacts in the same intent commit when fixing pointer drift.
- Driven by patch: `2026-02-25-10.42.md`.

## Patterns Identified
- Click-to-move regressions frequently come from mismatch between requested input coordinates and resolved navigation targets.
- Visual marker offsets can hide root-cause coordinate bugs and should stay opt-in, not default.
- Movement-system fixes are multi-file by nature (service + input + pointer) and should be staged atomically.
