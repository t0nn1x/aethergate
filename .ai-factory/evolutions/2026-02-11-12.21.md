# Evolution: 2026-02-11 12:21

## Intelligence Summary
- Patches analyzed: 0 (`.ai-factory/patches` not present)
- Evidence source: recent implementation and fix cycle on branch `refactor-player-pipeline-phase1`
- Key regressions observed:
  - Tap-to-move stopped working after service extraction (dependency lookup base issue)
  - Godot parse failures from strict typing (`Variant` inference with warnings-as-errors)
  - High risk during large script/folder migration (`.tscn` ext_resource path integrity)

## Improvements Applied

### ai-factory.fix
- Added Godot-specific investigation guard for strict typing inference traps.
- Added Godot-specific migration guard for `.tscn` references and `.uid` continuity.
- Added verification reminder to check `.tscn` resource existence after refactors.

### ai-factory.implement
- Added verification rules to check NodePath dependency resolution base (`self` vs `parent`).
- Added runtime smoke-check requirement for core loops (input/movement/state/chunk flow).

### ai-factory.task
- Added explicit Godot migration task requirements:
  - `.tscn` path rewiring
  - `.uid` continuity checks
  - behavior parity validation

### ai-factory.review
- Added Godot-specific review checklist:
  - NodePath resolution correctness
  - strict typing inference traps
  - scene/script reference integrity after moves
  - input/navigation behavior regression checks

### godot-development
- Added Aethergate-specific bounded-context conventions:
  - `src/Gameplay/Player`
  - `src/Gameplay/Navigation`
  - `src/World/Overworld`
  - `src/World/Streaming`
  - `src/Entities/Ui/Debug`
- Added rule to avoid reintroducing runtime orchestration scripts into `src/Map/Overworld`.

## Patterns Identified
- `#nodepath-resolution`: service dependency lookup base mismatch caused runtime no-op behavior
- `#gdscript-typing`: strict typing + inferred `Variant` can break parse when warnings are errors
- `#scene-rewire`: large folder refactors require explicit `.tscn` path validation

